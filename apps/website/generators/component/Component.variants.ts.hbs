import { tv, type VariantProps } from "tailwind-variants"

export const {{camelCase name}}Variants = tv({
  base: "",
})

export type {{pascalCase name}}VariantProps = VariantProps<typeof {{camelCase name}}Variants>
